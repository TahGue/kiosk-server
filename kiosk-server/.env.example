# Example environment configuration for kiosk-server
# Copy this file to .env and adjust values as needed

# Server settings
PORT=4000
NODE_ENV=development

# Force HTTPS redirection (effective only if implemented or behind a proxy)
FORCE_HTTPS=false

# CORS configuration (set to a specific origin or "*")
CORS_ORIGIN=*

# Static assets directory (relative to project root)
STATIC_DIR=public

# UI customization
KIOSK_TITLE=Kiosk Display
KIOSK_FOOTER_TEXT=© 2025 Kiosk System
TIMEZONE=Europe/Berlin

# Kiosk behavior flags used by frontend (optional)
DISABLE_CONTEXT_MENU=true
DISABLE_SHORTCUTS=true

# External URL to display in the kiosk iframe (set to blank to show nothing)
KIOSK_URL=https://example.com

# Optional: Admin token to protect control endpoints.
# If set, you must send header: x-admin-token: <value>
ADMIN_TOKEN=change-me-strong-token
