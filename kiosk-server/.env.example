# ============================================
# Kiosk Server Environment Configuration
# ============================================
# Copy this file to .env and adjust values
# 🟢 GOOD NEWS: Everything has defaults!
# 🟢 Minimum to start: Just copy and run!
# ============================================

# --- BASIC SETTINGS (Recommended to set) ---
PORT=4000
NODE_ENV=development

# Default URL to display on all kiosks
KIOSK_URL=https://example.com

# Force HTTPS redirection (effective only if implemented or behind a proxy)
FORCE_HTTPS=false

# CORS origin (use * for all origins, or specify a specific origin)
CORS_ORIGIN=*

# --- SECURITY SETTINGS (Optional, uncomment to customize) ---
# These protect your server from resource exhaustion attacks
# Uncomment and adjust based on your server capacity

# MAX_SSE_CLIENTS=100           # Max concurrent SSE connections
# MAX_HB_CLIENTS=200            # Max heartbeat clients tracked
# MAX_HEARTBEAT_RATE=120        # Heartbeats per minute per IP
# MAX_COMMAND_QUEUE_SIZE=100    # Max queued commands per client

# --- ADMIN PROTECTION (IMPORTANT for production!) ---
# If set, admin endpoints require 'x-admin-token' header
# Leave empty for testing, SET in production!
ADMIN_TOKEN=

# --- STATIC FILES ---
STATIC_DIR=public

# --- UI CUSTOMIZATION (Optional) ---
KIOSK_TITLE=Kiosk Display
KIOSK_FOOTER_TEXT=© 2025 Kiosk System
TIMEZONE=Europe/Berlin

# Kiosk behavior flags (disables right-click, shortcuts, etc.)
DISABLE_CONTEXT_MENU=true
DISABLE_SHORTCUTS=true

# --- SSH DEPLOYMENT SETTINGS (Optional) ---
# These prefill the admin dashboard deployment forms
# SERVER_BASE is the URL that clients will use to reach this server

# Example: http://192.168.1.10:4000 or https://kiosk.yourdomain.com
SERVER_BASE=

# Default SSH credentials (for convenience, NOT recommended for production)
# Use SSH keys instead of passwords!
DEFAULT_SSH_USERNAME=
DEFAULT_SSH_PASSWORD=

# --- THAT'S IT! ---
# The server will work with just these defaults
# For more details, see ENV-SETUP.md
