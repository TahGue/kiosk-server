# ============================================
# MUSTAQBAL.LOCAL - Optimized for 50-100 AntiX Clients
# ============================================
# Copy this to .env on your server
# This is pre-configured for your exact setup!
# ============================================

# --- SERVER SETTINGS (Your LAN) ---
PORT=4000
NODE_ENV=production

# Your server's LAN IP (change to your actual server IP)
# Example: 192.168.1.10
SERVER_BASE=http://192.168.1.10:4000

# --- DEFAULT KIOSK URL ---
# This is what all 50-100 clients will display by default
KIOSK_URL=https://www.mustaqbal.hb.local

# --- SECURITY (For LAN - relaxed settings) ---
# No admin token needed for internal LAN
ADMIN_TOKEN=

# Allow all origins since this is internal LAN
CORS_ORIGIN=*

# HTTPS not needed for internal LAN
FORCE_HTTPS=false

# --- CAPACITY OPTIMIZED FOR 100 DEVICES ---
# These limits are tuned for 50-100 AntiX clients
MAX_SSE_CLIENTS=120           # 100 clients + 20 admin/buffer
MAX_HB_CLIENTS=120            # Track up to 120 heartbeat clients
MAX_HEARTBEAT_RATE=60         # 1 heartbeat per second per client
MAX_COMMAND_QUEUE_SIZE=50     # Smaller queue, faster response

# --- UI CUSTOMIZATION ---
KIOSK_TITLE=Mustaqbal Display
KIOSK_FOOTER_TEXT=© 2025 Mustaqbal
TIMEZONE=Asia/Baghdad

# Disable context menu and shortcuts (kiosk mode)
DISABLE_CONTEXT_MENU=true
DISABLE_SHORTCUTS=true

# --- SSH DEPLOYMENT (Your AntiX Credentials) ---
# All your AntiX clients use these credentials
DEFAULT_SSH_USERNAME=tahar
DEFAULT_SSH_PASSWORD=tahar

# --- STATIC FILES ---
STATIC_DIR=public

# ============================================
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp env.mustaqbal.template .env
# 2. Edit SERVER_BASE with your actual server IP
# 3. Run: npm start
# ============================================
